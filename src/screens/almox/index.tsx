import { Text } from "react-native";
import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";

const AlmoxScreen = () => {
  return (
    <SafeAreaView>
      <Text>AlmoxScreen</Text>
    </SafeAreaView>
  );
};

export default AlmoxScreen;
